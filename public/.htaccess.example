# Necessary for URL rewriting
RewriteEngine On
RewriteBase /
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule (.*) index.php/$1 [L]

# Activate Shibboleth
# AuthType shibboleth
# Require shibboleth
# ShibRequestSetting requireSession 0